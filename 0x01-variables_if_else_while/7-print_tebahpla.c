#include <stdio.h>

/**
 * main - A program that prints letters from z to a
 * Return: Always (Success)
 */

int main(void)

{
	int a;

	for (a = 'z'; a<= 'a'; a++)
	putchar(a);
	putchar('\n');
	return (0);
}
